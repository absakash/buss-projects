import React from 'react';
import { useParams } from 'react-router-dom';

const PaymentFail = () => {
     
      return (
            <div>
                  failed to ranjection ....
            </div>
      );
};

export default PaymentFail;